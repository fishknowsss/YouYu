# 更新日志

本文件记录 YouYu 的重要版本变化。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，分类参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.6.0] - 2026-07-15

### 修复

- 修复专业模式设置页加入诊断导出后行列节奏断裂、按钮权重过重和状态内容挤压的问题；诊断工具现在使用独立整行，三行主设置与右侧动作保持统一间距和边缘。
- 修复后台登记、远程配置同步和流量上报在 Mihomo 已运行时始终复用本地代理，导致本地代理本身异常时无法尝试可用直连的问题。
- 修复 Node/Electron 网络错误只显示 `fetch failed`、丢失嵌套 DNS 或连接错误码的问题；诊断日志现在保留脱敏后的错误类型，便于区分 DNS、超时和拒绝连接。
- 修复更新保底只覆盖 `latest*.yml`、安装包或 `.blockmap` 下载失败后不会换路的问题；元数据检查和安装包下载现在分别执行连接保底。

### 改进

- 诊断栏统一容纳常见问题提示、当前会话日志条数和低权重“导出”动作；在 `900×600` 下覆盖无异常、长错误、0/200 条日志、导出中和更新下载状态，页面不产生滚动或错位。
- 诊断日志保存对话框默认打开用户“下载”文件夹，仍允许用户自行改名或选择其他位置。
- 后台通信使用不继承系统代理的应用内独立会话：先直连 Cloudflare Worker，只有可恢复的传输故障或临时网关故障才尝试正在运行的本地 Mihomo；鉴权、限流、无效响应和用户取消不会换路。
- Cloudflare Worker 增加由 Cloudflare 托管 DNS 与证书的 `youyu-api.fishknowsss.com` 生产自定义域名，客户端不再把 `workers.dev` 作为唯一后台入口。
- 直连与本地代理共用一次总超时预算，并为第二条线路预留时间；重试复用同一请求体、报告 ID、设备种子和签名，最终诊断只记录两条线路的脱敏结果。
- 启用 Electron 内置解析器、Happy Eyeballs 和 secure DNS，并配置 DNSPod、阿里云与 Cloudflare 四个 DoH 入口；解析保底只作用于 YouYu，不修改 Windows DNS 或 Hosts，也不接受被污染的系统解析结果。
- 自动更新改用 GitHub `releases/latest/download` 的稳定通道入口，标准版、内部通道版和无桌宠版仍分别读取 `latest.yml`、`latest-in.yml` 和 `latest-no.yml`。检查和后台下载都先直连，连接失败时刷新应用内 DNS，并在本地 Mihomo 可用时换路重试。
- 诊断状态和软件更新状态增加常驻读屏播报区域；禁用按钮不再出现可点击的悬停反馈。

### 安全与边界

- 不写入固定 GitHub 或 Cloudflare IP，不修改系统 Hosts，不关闭 TLS 证书校验，也不执行系统级 DNS 重置。
- 生产自定义域名与旧 `workers.dev` 入口指向同一 Cloudflare Worker，并非跨服务商镜像；新的保底不能绕过域名、SNI 或整个 Cloudflare 网络被完全封锁的情况。
- 本地交付继续只留存带私有订阅的 `-in`、`-no` 双包；公开三个更新通道全部使用空内置订阅构建。

### 验证

- 为后台直连优先、代理回退、共享超时、取消竞态、双线路脱敏错误、请求体与签名复用、更新元数据与下载双阶段回退补充回归覆盖。
- 为诊断栏结构、0/200 条边界、长状态、导出忙碌态、更新状态播报、禁用悬停和下载目录补充回归覆盖。
- 设置页在 `900×600` 下完成常规、错误、压力和下载状态多轮浏览器验收，右侧动作边缘一致且无横向或纵向溢出。
- 发布闭环覆盖桌面端与 Worker 校验、Windows 三通道构建、私有双包反向校验、公开包空订阅检查、安装包冒烟测试以及远端 `latest*.yml` 与资产哈希复核。

## [1.5.11] - 2026-07-15

### 修复

- 修复托盘“网络修复”子菜单同时提供“重启内核并重启软件”和“重型网络修复”、操作边界不清晰的问题；现在合并为单一“网络修复”入口。
- 修复专业模式设置页在运行状态异常但仍保留有效运行意图时无法恢复 Mihomo 的问题；修复完成后会按修复前的运行意图决定是否启动新内核。
- 修复 Windows 原生网络命令失败时可能未被识别，以及 Microsoft Store 回环豁免未在写入后复核的问题。
- 修复设置页修复状态与使用登记状态中的圆环动画计时正常、画面却保持静止的问题。
- 修复设置页的“已同步”“已保存”“已修复”等成功状态进入首页诊断区域的问题；这些提示现在只在设置页短暂显示。

### 改进

- 将网络修复拆为安全阶段：先关闭并回读确认 WinINet 代理，再停止 Mihomo，随后清理代理字符串、WinHTTP 代理、DNS 缓存和 Mihomo 易失缓存，并修复 Microsoft Store 回环豁免。
- 托盘“网络修复”完成清理后不会在旧进程中间启动一次内核，而是直接退出并重启 YouYu，再由新进程启动使用新端口的 Mihomo。
- 专业模式设置页“修复”不会重启 YouYu；原本运行时会使用新端口重启 Mihomo，原本停止时保持停止，异常状态下仍有有效运行意图时可以恢复运行。
- 修复期间暂停可能重新拉起内核或改写状态的后台任务，并对系统代理启用结果增加回读确认。
- 不执行 Winsock、TCP/IP 或 DHCP 重置，避免破坏 VPN、虚拟网卡和静态 IP 配置，也避免引入必须重启 Windows 的修复步骤。
- 专业模式设置页新增当前会话诊断日志条数和“导出”按钮，可生成经过脱敏的 UTF-8 文本，便于排查疑难与罕见问题。
- 诊断导出最多包含当前会话 200 条记录，采用字段白名单并对日志文本脱敏，不写入完整订阅、凭据等敏感设置。
- 设置页诊断行会根据 `lastError` 以红色显示“常见问题类型 · 建议操作”，并在修复前为系统代理、DNS、内核、网络连接或订阅问题执行对应的低风险预处理。
- 针对性预处理的单项失败会记录诊断并继续确定性的完整安全修复链；不会按错误关键词增加 Winsock、TCP/IP、DHCP 等高风险分支，成功修复后清除旧错误提示。
- 本地交付只把带私有订阅的 `-in`、`-no` 安装包及对应 `.blockmap` 留存在 `local-subscription-builds/<version>/`；公开三个更新通道仍全部使用空内置订阅构建，私有双包不上传。

### 验证

- 为分阶段系统修复、运行意图恢复、托盘重启顺序、Mihomo 易失缓存清理和圆环动画补充回归覆盖。
- 为诊断日志计数、导出脱敏、取消导出、常见问题分类、针对性预处理顺序、修复成功清除错误及设置页成功状态隔离补充回归覆盖。
- 发布闭环覆盖桌面端与 Worker 校验、Windows 三通道构建、安装包结构与空内置订阅检查、界面关键尺寸检查和冒烟测试。
- 公开更新资产与本地私有双包分别构建和留存，避免同名 `-in`、`-no` 安装包互相混用。

## [1.5.10] - 2026-07-13

### 修复

- 修复完整订阅中的 `proxy-groups[*].url` 和 `proxy-providers.*.health-check.url` 可能保留常见 HTTP 204 探测地址，导致 Mihomo 统一延迟测速出现第二次请求超时与诊断警告的问题。
- 修复在线订阅与缓存回退生成路径行为不一致的隐患；两条路径现在都会在写入运行时配置前完成相同规范化。

### 改进

- 将 `http://www.gstatic.com/generate_204` 转换为对应 HTTPS 地址，将 `http://1.1.1.1/generate_204` 转换为 Cloudflare HTTPS 204 地址。
- 对规范化后的订阅健康检查及 YouYu 自建健康检查统一要求返回 204，避免异常页面被误判为节点可用。
- 仅处理健康检查字段中的已知明文地址，保留 provider 订阅下载地址和自定义 HTTPS 健康检查配置。

### 验证

- 主项目完整测试、类型检查、ESLint、Prettier 和生产构建通过。
- 在线订阅、缓存回退、group、provider、交叉地址映射和自定义配置保留均有回归覆盖。
- 三个公开更新通道安装包通过空内置订阅校验、安装包结构校验和冒烟检查。
- 本版本未修改 `src/renderer` 或前端样式，界面与交互保持不变。

## [1.5.9] - 2026-07-13

### 安全

- 净化 Git 历史中的 Wrangler 本地状态、历史内部安装包、构建缓存和 Python 字节码，避免本地数据库、内部构建内容与生成文件继续通过旧提交传播。
- 增加仓库卫生校验，CI 会阻止 `.wrangler`、本地订阅、发布产物、缓存、临时环境文件和常见凭据形态进入仓库。
- 覆盖安装只处理当前安装目录对应的 YouYu 进程，并在强制关闭前验证和恢复由 YouYu 实际接管的系统代理，降低误关其他安装与残留无效代理的风险。
- 公开标准版、内部更新通道版和无桌宠通道版统一使用空内置订阅构建；私有订阅只允许存在于本地忽略文件和本地交付产物中。

### 修复

- 修复停止代理后，旧的健康检查、后台刷新或恢复任务可能重新拉起 Mihomo 的竞态。
- 修复退出、重启、更新安装和异常清理之间的状态交接，确保清理失败时可以恢复运行，安装器启动失败时保留已下载更新供重试。
- 修复用户或设备身份切换时，流量队列、远程配置缓存和远程订阅可能沿用旧身份的问题。
- 修复设备失效后远程订阅未及时清除的问题；失效设备现在会停止代理并清理对应远程状态。
- 修复 Mihomo 进程退出状态未完整传递、关闭过程中重复操作，以及启动任务校验不足的问题。
- 修复系统代理所有权判断与恢复边界，避免清理并非由 YouYu 设置的代理。
- 修复流量持久化、上报确认、重试与运行时身份切换中的一致性问题。
- 修复 Worker 激活与登记流程的并发幂等、限流原子性和失败回滚问题。

### 改进

- 将运行意图、应用重启、启动任务、远程订阅、流量登记和身份交接拆分为职责清晰的模块，并补齐跨模块测试。
- Worker 对 JSON 请求体、配置条目数量与长度设置明确上限，已识别但无效的配置字段会被拒绝，而不是静默忽略。
- 新增可审计的 D1 迁移工具，支持远端/本地 `check`、`dry-run` 和显式 `apply`，写入前校验核心表、列、主键、唯一约束和索引。
- 统一 Windows 构建模式与三通道发布编排，降低标准版、内部更新通道版和无桌宠通道版互相污染的风险。
- 扩充 GitHub Actions 发布门禁，加入仓库卫生、Worker、版本标签和公开包订阅校验。
- 完善 README、发布流程、安全策略、历史净化记录和版本发布说明。

### 验证

- 主项目 36 个测试文件、289 项测试通过。
- Worker 29 项测试通过。
- 仓库卫生、桌面端与 Worker 类型检查、ESLint、Prettier、Worker dry-run、桌面端生产构建和安装包冒烟检查通过。
- 三个公开更新通道的安装包、`.blockmap` 和 `latest*.yml` 均由同一次 `dist:win:release` 构建产生并通过空内置订阅校验。
- 本版本未修改 `src/renderer` 或前端样式，界面与交互保持不变。

[1.6.0]: https://github.com/fishknowsss/YouYu/releases/tag/v1.6.0
[1.5.11]: https://github.com/fishknowsss/YouYu/releases/tag/v1.5.11
[1.5.10]: https://github.com/fishknowsss/YouYu/releases/tag/v1.5.10
[1.5.9]: https://github.com/fishknowsss/YouYu/releases/tag/v1.5.9
